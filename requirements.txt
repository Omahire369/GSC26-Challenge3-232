# Minimum versions the pipeline is written against.
# For a byte-identical reproduction of the submitted artifacts, use the pinned
# set in requirements-lock.txt instead.
pandas>=2.0
numpy>=1.24
pyarrow>=14.0
scikit-learn>=1.3
lightgbm>=4.0
xgboost>=2.0
joblib>=1.3
pytest>=7.0
