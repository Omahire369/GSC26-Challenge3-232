# Exact versions used to train the submitted models and produce submission.csv,
# on Python 3.11.4 / Windows. Model fits are seeded, so this set reproduces the
# artifacts in artifacts/models deterministically.
numpy==2.4.6
pandas==3.0.5
pyarrow==25.0.0
scikit-learn==1.9.0
lightgbm==4.7.0
xgboost==3.2.0
joblib==1.5.3
pytest==9.1.1
